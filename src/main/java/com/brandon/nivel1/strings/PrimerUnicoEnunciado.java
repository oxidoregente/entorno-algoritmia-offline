package com.brandon.nivel1.strings;

/**
 * RETO: Primer Carácter Único.
 * 
 * Dado un string, encuentra el primer carácter que no se repite y devuelve su índice.
 * Si no existe, devuelve -1.
 * 
 * Ejemplo: "leetcode" -> 0 (la 'l' es la primera única)
 * Ejemplo: "loveleetcode" -> 2 (la 'v' es la primera única)
 */
public class PrimerUnicoEnunciado {

    public int encontrar(String s) {
        // TODO: Implementa tu lógica aquí. 
        // Pista: Usa una tabla de frecuencias (HashMap o array de 256).
        return -1;
    }
}
