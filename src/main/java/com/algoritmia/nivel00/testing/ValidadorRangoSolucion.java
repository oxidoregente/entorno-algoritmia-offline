package com.algoritmia.nivel00.testing;

/**
 * Nivel 00: Testing Inverso.
 */
public class ValidadorRangoSolucion {

    /**
     * Verifica si un número está dentro de un rango inclusivo.
     */
    public boolean estaEnRango(int numero, int min, int max) {
        return numero >= min && numero <= max;
    }
}
