package com.algoritmia.nivel02.streams;

import java.util.List;

/**
 * RETO: Segundo Número Más Grande.
 * 
 * Dada una lista de enteros, encuentra el segundo número más grande
 * utilizando exclusivamente la API de Streams.
 */
public class SegundoMasGrandeEnunciado {

    public Integer encontrar(List<Integer> numeros) {
        // TODO: Implementa tu lógica aquí.
        // Pista: .distinct(), .sorted(Comparator.reverseOrder()), .skip(1), .findFirst()
        return null;
    }
}
