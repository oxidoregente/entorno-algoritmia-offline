package com.algoritmia.nivel01.arrays;

/**
 * 🎓 RETO: Mover Ceros al Final.
 * <b>Dificultad: Fácil</b>
 *
 * <p>Dado un array de enteros `nums`, mueve todos los ceros (0) al final del array
 * manteniendo el orden relativo de los elementos que no son cero.</p>
 *
 * <p><b>Restricción:</b> Debes hacer la operación "in-place", modificando el array original
 * sin crear una copia del mismo.</p>
 *
 * <h3>Ejemplo:</h3>
 * <pre>
 * Entrada: nums = [0, 1, 0, 3, 12]
 * Salida:  [1, 3, 12, 0, 0]
 * </pre>
 *
 * <p><b>Pistas:</b></p>
 * <ul>
 *   <li>Usa el enfoque de "puntero de escritura" (two-pointer).</li>
 *   <li>Recorre el array y cada vez que encuentres un número distinto de cero,
 *       colócalo en la posición del puntero y avanza el puntero.</li>
 *   <li>Al final, rellena las posiciones restantes con ceros.</li>
 * </ul>
 */
public class MoverCerosEnunciado {

    /**
     * Mueve todos los ceros al final del array manteniendo el orden relativo de los no-ceros.
     * La operación se realiza in-place.
     *
     * @param nums El array de enteros a modificar. Puede ser nulo o vacío.
     */
    public void mover(int[] nums) {
        // TODO: Implementa el movimiento de ceros in-place
    }
}
