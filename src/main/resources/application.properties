# Configuración Base
spring.application.name=entorno-algoritmia-offline

# Perfil activo por defecto
spring.profiles.active=dev

# Configuración de Jackson para JSON
spring.jackson.serialization.indent_output=true
